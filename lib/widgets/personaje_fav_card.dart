import 'dart:html';
import 'package:flutter/material.dart';

class PersonajeFavCard extends StatelessWidget {
  const PersonajeFavCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
